# Token com permissão de repo para disparar repository_dispatch (tracking de visitas)
# Crie um PAT em https://github.com/settings/tokens (escopo: repo) e adicione no
# GitHub Actions secrets, ou exponha como VITE_GH_TOKEN para builds locais.
VITE_GH_TOKEN=
