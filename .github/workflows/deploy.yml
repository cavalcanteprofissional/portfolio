name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect changes in resume pipeline
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            resume:
              - 'resume/**'

      - name: Restore generated CVs
        id: cv
        uses: actions/cache@v4
        with:
          path: public/cv
          key: cvs-${{ hashFiles('resume/**') }}

      - name: Setup Python
        if: steps.filter.outputs.resume == 'true' || steps.cv.outputs.cache-hit != 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache Hugging Face models
        if: steps.filter.outputs.resume == 'true' || steps.cv.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: hf-mbart-${{ runner.os }}
          restore-keys: hf-mbart-

      - name: Install Python dependencies
        if: steps.filter.outputs.resume == 'true' || steps.cv.outputs.cache-hit != 'true'
        run: |
          pip install --upgrade pip
          pip install transformers torch sentencepiece sacremoses jinja2 python-frontmatter pyyaml playwright protobuf tiktoken

      - name: Install Playwright Chromium
        if: steps.filter.outputs.resume == 'true' || steps.cv.outputs.cache-hit != 'true'
        run: playwright install --with-deps chromium

      - name: Generate resume PDFs
        if: steps.filter.outputs.resume == 'true' || steps.cv.outputs.cache-hit != 'true'
        run: python resume/build.py
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
